/**
 * investor-relations-contacts controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::investor-relations-contacts.investor-relations-contacts');
